<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Anime List</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <div class="container mx-auto px-4 py-8">
        <h1 class="text-4xl font-bold mb-8">Anime List</h1>
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
            <% animes.forEach(anime => { %>
                <a href="/anime/<%= anime.id %>" class="block bg-white rounded-lg shadow-md overflow-hidden hover:shadow-lg transition">
                    <img src="<%= anime.imageUrl %>" alt="<%= anime.name %>" class="w-full h-48 object-cover">
                    <div class="p-4">
                        <h2 class="text-xl font-semibold mb-2"><%= anime.name %></h2>
                        <p class="text-gray-600 text-sm"><%= anime.episodes.length %> episodes</p>
                    </div>
                </a>
            <% }); %>
        </div>
        <div class="mt-8">
            <a href="/anime/upload" class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded">
                Upload New Anime
            </a>
        </div>
    </div>
</body>
</html>