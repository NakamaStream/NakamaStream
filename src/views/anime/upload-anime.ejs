<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NakamaStream - Subir Anime</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Nunito:wght@300;400;600;700&display=swap');
        body {
            font-family: 'Nunito', sans-serif;
            background-image: url('https://www.transparenttextures.com/patterns/cubes.png');
            background-color: #f3f4f6;
        }
        .custom-shadow {
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
        }
        .input-focus {
            transition: all 0.3s ease-in-out;
        }
        .input-focus:focus {
            box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.5);
        }
        .anime-bg {
            background-image: url('https://github.com/NakamaStream/Resources/blob/main/img/upload-anime.png?raw=true');
            background-size: cover;
            background-position: center;
        }
    </style>
</head>
<body class="min-h-screen flex items-center justify-center p-4">
    <div class="container mx-auto max-w-4xl">
        <div class="bg-white rounded-3xl shadow-2xl overflow-hidden custom-shadow">
            <div class="md:flex">
                <div class="md:w-1/3 anime-bg p-8 text-black">
                    <h2 class="text-3xl font-bold mb-4">Subir Anime</h2>
                    <p class="mb-4">¡Comparte tu anime favorito con la comunidad!</p>
                    <ul class="space-y-2">
                        <li><i class="fas fa-check-circle mr-2"></i> Fácil de usar</li>
                        <li><i class="fas fa-check-circle mr-2"></i> Vista previa instantánea</li>
                        <li><i class="fas fa-check-circle mr-2"></i> Cargas de alta calidad</li>
                    </ul>
                </div>                
                <div class="md:w-2/3 p-8">
                    <h1 class="text-4xl font-bold mb-8 text-center text-gray-800">Subir un nuevo Anime</h1>
                    <form action="/anime/upload" method="POST" class="space-y-6">
                        <div>
                            <label class="block text-gray-700 text-sm font-semibold mb-2" for="name">
                                <i class="fas fa-film mr-2"></i>Nombre del anime
                            </label>
                            <input class="w-full px-4 py-3 rounded-lg border-2 border-gray-300 focus:outline-none focus:border-blue-500 input-focus" id="name" type="text" name="name" required placeholder="Introduzca el nombre del anime">
                        </div>
                        <div>
                            <label class="block text-gray-700 text-sm font-semibold mb-2" for="imageUrl">
                                <i class="fas fa-image mr-2"></i>Image URL
                            </label>
                            <input class="w-full px-4 py-3 rounded-lg border-2 border-gray-300 focus:outline-none focus:border-blue-500 input-focus" id="imageUrl" type="url" name="imageUrl" required placeholder="Introduzca la URL de la imagen">
                        </div>
                        <div>
                            <label class="block text-gray-700 text-sm font-semibold mb-2" for="description">
                                <i class="fas fa-align-left mr-2"></i>Descripción
                            </label>
                            <textarea class="w-full px-4 py-3 rounded-lg border-2 border-gray-300 focus:outline-none focus:border-blue-500 input-focus" id="description" name="description" rows="4" required placeholder="Introduzca la descripción del anime"></textarea>
                        </div>
                        <div>
                            <label class="block text-gray-700 text-sm font-semibold mb-2" for="episodes">
                                <i class="fas fa-list-ol mr-2"></i>Episodios
                            </label>
                            <input class="w-full px-4 py-3 rounded-lg border-2 border-gray-300 focus:outline-none focus:border-blue-500 input-focus" id="episodes" type="text" name="episodes" required placeholder="Introduzca el episodio (solo ponga un 1)">
                        </div>
                        <div>
                            <label class="block text-gray-700 text-sm font-semibold mb-2" for="videoUrl">
                                <i class="fas fa-video mr-2"></i>Video URL
                            </label>
                            <input class="w-full px-4 py-3 rounded-lg border-2 border-gray-300 focus:outline-none focus:border-blue-500 input-focus" id="videoUrl" type="url" name="videoUrl" placeholder="Introduzca la URL del vídeo">
                            <button type="button" id="previewButton" class="mt-3 bg-green-500 hover:bg-green-600 text-white font-bold py-3 px-6 rounded-lg transition duration-300 ease-in-out transform hover:-translate-y-1 hover:scale-105 focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-opacity-50">
                                <i class="fas fa-play mr-2"></i>Vista previa del vídeo
                            </button>
                        </div>
                        <div id="videoPreview" class="hidden mt-6">
                            <h3 class="text-xl font-semibold mb-3 text-gray-800">Vista previa del vídeo</h3>
                            <div class="aspect-w-16 aspect-h-9 rounded-xl overflow-hidden custom-shadow">
                                <iframe id="videoFrame" src="" frameborder="0" allow="autoplay; fullscreen; picture-in-picture" allowfullscreen class="w-full h-full"></iframe>
                            </div>
                        </div>
                        <div class="flex items-center justify-between pt-6">
                            <button class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-3 px-8 rounded-lg transition duration-300 ease-in-out transform hover:-translate-y-1 hover:scale-105 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50" type="submit">
                                <i class="fas fa-upload mr-2"></i>Subir Anime
                            </button>
                            <a href="/anime" class="inline-block font-bold text-lg text-blue-500 hover:text-blue-700 transition duration-300 hover:underline">
                                <i class="fas fa-arrow-left mr-2"></i>Volver a la lista de animes
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <script>
        document.getElementById('previewButton').addEventListener('click', function() {
            const videoUrl = document.getElementById('videoUrl').value;
            const videoFrame = document.getElementById('videoFrame');
            const videoPreview = document.getElementById('videoPreview');

            if (videoUrl) {
                videoFrame.src = videoUrl;
                videoPreview.classList.remove('hidden');
                videoPreview.scrollIntoView({behavior: 'smooth'});
            } else {
                alert('Please enter a valid video URL');
            }
        });
    </script>
</body>
</html>