<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload Anime</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap');
        body {
            font-family: 'Poppins', sans-serif;
        }
        .custom-shadow {
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
        }
        .input-focus {
            transition: all 0.3s ease-in-out;
        }
        .input-focus:focus {
            box-shadow: 0 0 0 3px rgba(66, 153, 225, 0.5);
        }
    </style>
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center">
    <div class="container mx-auto px-4 py-8 max-w-3xl">
        <div class="bg-white rounded-3xl shadow-xl p-8 custom-shadow">
            <h1 class="text-5xl font-bold mb-10 text-center text-gray-800">Subir Nuevo Anime</h1>
            <form action="/anime/upload" method="POST" class="space-y-6">
                <div>
                    <label class="block text-gray-700 text-sm font-semibold mb-2" for="name">
                        Nombre
                    </label>
                    <input class="w-full px-4 py-3 rounded-lg border-2 border-gray-300 focus:outline-none focus:border-blue-500 input-focus" id="name" type="text" name="name" required>
                </div>
                <div>
                    <label class="block text-gray-700 text-sm font-semibold mb-2" for="imageUrl">
                        Image URL
                    </label>
                    <input class="w-full px-4 py-3 rounded-lg border-2 border-gray-300 focus:outline-none focus:border-blue-500 input-focus" id="imageUrl" type="url" name="imageUrl" required>
                </div>
                <div>
                    <label class="block text-gray-700 text-sm font-semibold mb-2" for="description">
                        Description
                    </label>
                    <textarea class="w-full px-4 py-3 rounded-lg border-2 border-gray-300 focus:outline-none focus:border-blue-500 input-focus" id="description" name="description" rows="4" required></textarea>
                </div>
                <div>
                    <label class="block text-gray-700 text-sm font-semibold mb-2" for="episodes">
                        Episodes (comma-separated)
                    </label>
                    <input class="w-full px-4 py-3 rounded-lg border-2 border-gray-300 focus:outline-none focus:border-blue-500 input-focus" id="episodes" type="text" name="episodes" required>
                </div>
                <div>
                    <label class="block text-gray-700 text-sm font-semibold mb-2" for="videoUrl">
                        Video URL
                    </label>
                    <input class="w-full px-4 py-3 rounded-lg border-2 border-gray-300 focus:outline-none focus:border-blue-500 input-focus" id="videoUrl" type="url" name="videoUrl">
                    <button type="button" id="previewButton" class="mt-3 bg-green-500 hover:bg-green-600 text-white font-bold py-3 px-6 rounded-lg transition duration-300 ease-in-out transform hover:-translate-y-1 hover:scale-105 focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-opacity-50">
                        Preview Video
                    </button>
                </div>
                <div id="videoPreview" class="hidden mt-6">
                    <h3 class="text-xl font-semibold mb-3 text-gray-800">Video Preview</h3>
                    <div class="aspect-w-16 aspect-h-9 rounded-xl overflow-hidden custom-shadow">
                        <iframe id="videoFrame" src="" frameborder="0" allow="autoplay; fullscreen; picture-in-picture" allowfullscreen class="w-full h-full"></iframe>
                    </div>
                </div>
                <div class="flex items-center justify-between pt-6">
                    <button class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-3 px-8 rounded-lg transition duration-300 ease-in-out transform hover:-translate-y-1 hover:scale-105 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50" type="submit">
                        Subir Anime
                    </button>
                    <a href="/anime" class="inline-block font-bold text-lg text-blue-500 hover:text-blue-700 transition duration-300 hover:underline">
                        Back to Anime List
                    </a>
                </div>
            </form>
        </div>
    </div>

    <script>
        document.getElementById('previewButton').addEventListener('click', function() {
            const videoUrl = document.getElementById('videoUrl').value;
            const videoFrame = document.getElementById('videoFrame');
            const videoPreview = document.getElementById('videoPreview');

            if (videoUrl) {
                videoFrame.src = videoUrl;
                videoPreview.classList.remove('hidden');
                videoPreview.scrollIntoView({behavior: 'smooth'});
            } else {
                alert('Please enter a valid video URL');
            }
        });
    </script>
</body>
</html>