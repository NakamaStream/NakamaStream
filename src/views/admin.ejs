<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NakamaStream - Admin Panel</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.9.3/css/bulma.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        body {
            font-family: 'Montserrat', sans-serif;
            background-color: #f5f5f5;
        }
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
        }
        .title {
            color: #4a4a4a;
            font-weight: 700;
            margin-bottom: 2rem;
        }
        .panel {
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 2px 3px rgba(10,10,10,.1), 0 0 0 1px rgba(10,10,10,.1);
            padding: 2rem;
            margin-bottom: 2rem;
        }
        .panel-heading {
            font-weight: bold;
            margin-bottom: 1.5rem;
        }
        .buttons {
            display: flex;
            flex-wrap: wrap;
            gap: 1rem;
        }
        .button {
            font-weight: 700;
            transition: background-color 0.3s ease;
        }
        .button.is-danger {
            background-color: #ff3860;
            border-color: transparent;
            color: #fff;
        }
        .button.is-danger:hover {
            background-color: #ff1f4b;
        }
        .button.is-warning {
            background-color: #ffdd57;
            border-color: transparent;
            color: rgba(0,0,0,.7);
        }
        .button.is-warning:hover {
            background-color: #ffd000;
        }
        .button.is-primary {
            background-color: #00d1b2;
            border-color: transparent;
            color: #fff;
        }
        .button.is-primary:hover {
            background-color: #00bea4;
        }
        .field:not(:last-child) {
            margin-bottom: 1.5rem;
        }
    </style>
</head>
<body>
    <div class="container">
        <% if (req.session.isAdmin) { %>
            <h1 class="title">Panel de Administrador</h1>
            <div class="panel">
                <h2 class="panel-heading">Acciones de Ban</h2>
                <form action="/admin/ban-user" method="POST">
                    <div class="field">
                        <label class="label">ID de usuario</label>
                        <div class="control">
                            <input class="input" type="number" name="userId" placeholder="ID de usuario" required>
                        </div>
                    </div>
                    <div class="field">
                        <div class="control">
                            <div class="select">
                                <select name="banType">
                                    <option value="temporary">Temporal</option>
                                    <option value="permanent">Permanente</option>
                                </select>
                            </div>
                        </div>
                    </div>
                    <div class="field">
                        <div class="control">
                            <input class="input" type="number" name="banDuration" placeholder="Duración del ban en días" required>
                        </div>
                    </div>
                    <div class="field">
                        <div class="control">
                            <button class="button is-warning" type="submit">Banear Usuario</button>
                        </div>
                    </div>
                </form>
            </div>
            <div class="panel">
                <h2 class="panel-heading">Cambiar Datos de Usuario</h2>
                <form action="/admin/update-user" method="POST">
                    <div class="field">
                        <label class="label">ID de usuario</label>
                        <div class="control">
                            <input class="input" type="number" name="userId" placeholder="ID de usuario" required>
                        </div>
                    </div>
                    <div class="field">
                        <div class="control">
                            <input class="input" type="text" name="username" placeholder="Nuevo nombre de usuario" required>
                        </div>
                    </div>
                    <div class="field">
                        <div class="control">
                            <input class="input" type="email" name="email" placeholder="Nuevo correo electrónico" required>
                        </div>
                    </div>
                    <div class="field">
                        <div class="control">
                            <input class="input" type="password" name="password" placeholder="Nueva contraseña" required>
                        </div>
                    </div>
                    <div class="field">
                        <div class="control">
                            <button class="button is-primary" type="submit">Actualizar Datos de Usuario</button>
                        </div>
                    </div>
                </form>
            </div>
            <div class="panel">
                <h2 class="panel-heading">Promover Usuario a Administrador</h2>
                <form action="/admin/promote-admin" method="POST">
                    <div class="field">
                        <label class="label">ID de usuario</label>
                        <div class="control">
                            <input class="input" type="number" name="userId" placeholder="ID de usuario" required>
                        </div>
                    </div>
                    <div class="field">
                        <div class="control">
                            <button class="button is-primary" type="submit">Promover a Administrador</button>
                        </div>
                    </div>
                </form>
            </div>
        <% } else { %>
            <p>No tienes permisos de administrador.</p>
        <% } %>
    </div>
</body>
</html>
