<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= anime.name %></title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js" defer></script>
</head>
<body class="bg-gray-100" x-data="{ selectedEpisode: null }">
    <div class="container mx-auto px-4 py-8">
        <h1 class="text-4xl font-bold mb-6"><%= anime.name %></h1>
        <div class="flex flex-col md:flex-row gap-8">
            <div class="md:w-1/3">
                <img src="<%= anime.imageUrl %>" alt="<%= anime.name %>" class="w-full rounded-lg shadow-lg">
            </div>
            <div class="md:w-2/3">
                <p class="text-lg mb-6"><%= anime.description %></p>
                
                <h2 class="text-2xl font-semibold mb-4">Episodes</h2>
                <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 gap-4 mb-6">
                    <% episodes.forEach((episode, index) => { %>
                        <button 
                            @click="selectedEpisode = <%= index %>"
                            class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded transition"
                        >
                            Episode <%= index + 1 %>
                        </button>
                    <% }) %>
                </div>

                <div x-show="selectedEpisode !== null" class="mt-6">
                    <h3 class="text-xl font-semibold mb-2">Episode <span x-text="selectedEpisode + 1"></span></h3>
                    <template x-if="<%= JSON.stringify(episodes) %>[selectedEpisode]">
                        <div class="aspect-w-16 aspect-h-9">
                            <iframe :src="<%= JSON.stringify(episodes) %>[selectedEpisode].video_url" frameborder="0" allowfullscreen class="w-full h-full"></iframe>
                        </div>
                    </template>
                    <template x-if="!<%= JSON.stringify(episodes) %>[selectedEpisode]">
                        <p class="text-red-500">Este Episodio no está disponible.</p>
                    </template>
                </div>
            </div>
        </div>
        <div class="mt-8 flex justify-between items-center">
            <a href="/anime" class="inline-block px-6 py-2 bg-blue-500 text-white rounded hover:bg-blue-600 transition">Back to Anime List</a>
            <a href="/anime/<%= anime.id %>/upload-episode" class="inline-block px-6 py-2 bg-green-500 text-white rounded hover:bg-green-600 transition">Upload New Episode</a>
        </div>
    </div>
</body>
</html>